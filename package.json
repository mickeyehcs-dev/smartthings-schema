{
  "name": "smartthings-schema",
  "version": "1.0.0",
  "description": "SmartThings Schema Connector running on Cloudflare Workers",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.25.0"
  }
}
